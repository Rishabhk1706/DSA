https://leetcode.com/problems/number-of-1-bits/ 14-07-26

class Solution {
    public int hammingWeight(int n) {
        String s = Integer.toBinaryString(n);
        int c=0;
        for(char ch : s.toCharArray()){
            if(ch=='1') c++;
        } 
        return c;
    }
}